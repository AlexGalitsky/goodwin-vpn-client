import NetworkExtension
import TrustTunnelClient

/// Official TrustTunnel Packet Tunnel (P-T2). Same pattern as
/// refs/trusttunnel-client/macos/Extension — do not mix with SOCKS/hev.
class PacketTunnelProvider: AGPacketTunnelProvider {}

import NetworkExtension
import TrustTunnelClient

/// Official TrustTunnel Packet Tunnel (P-T2 / P-I4). Same pattern as macOS Extension.
/// Do not mix with SOCKS / hev.
class PacketTunnelProvider: AGPacketTunnelProvider {}

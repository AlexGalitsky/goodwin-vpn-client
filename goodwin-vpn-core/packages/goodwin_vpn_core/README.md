# goodwin_vpn_core

Pure Dart: share-link parsing, profiles, Xray/Hysteria JSON, FFI, `VpnCore` (SOCKS only).

TrustTunnel profiles are parsed here; the engine lives in the Flutter app (`vpn_plugin`).

```powershell
cd packages\goodwin_vpn_core
dart test
```

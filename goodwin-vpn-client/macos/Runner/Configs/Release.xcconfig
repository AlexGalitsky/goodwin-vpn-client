#include "../../Flutter/Flutter-Release.xcconfig"
#include "Warnings.xcconfig"

// Inherited by Extension / SocksTunnel (project-level Release + Profile).
EXCLUDED_ARCHS = x86_64
EXCLUDED_ARCHS[sdk=macosx*] = x86_64
